# VSDC / EBM test cases (template for RRA certification)

Fill one row per scenario. Attach redacted request/response logs from sandbox.

| ID | Scenario | Preconditions | Steps | Expected | Evidence (log ref) | Pass/Fail |
|----|-----------|---------------|-------|----------|-------------------|------------|
| TC-01 | Standard sale, all STANDARD VAT | Org TIN + device fields set, `ENABLE_EBM=true` | Complete POS sale | `EbmTransaction` SUCCESS, PDF shows EBM block | 2026-08-10, local sandbox (`localhost:8080/vsdc`), `saveInvc()` direct call, invcNo 97308 → resultCd 000, rcptNo 45, sdcId SDC012000250 | **PASS** |
| TC-02 | Mixed tax categories | Products with STANDARD / ZERO_RATED / EXEMPT | Complete sale | Line tax codes match RRA mapping (A/B/D) | 2026-08-10, local sandbox, 3-line sale (B/C/A) → resultCd 000, rcptNo 46 | **PASS** |
| TC-03 | B2B customer TIN | Corporate customer with TIN | Complete sale | Customer TIN in gateway payload | 2026-08-10, local sandbox, business TIN w/o `prcOrdCd` → correctly rejected, resultCd 910 "prcOrdCd: size must be between 6 and 6" | **PASS** (rejection confirms mandatory 6-char purchase code; app must collect `prcOrdCd` before submitting B2B sales — see note below) |
| TC-04 | Gateway timeout / 5xx | Mock or sandbox fault injection | Complete sale | `ebm_queue` row, retry job eventually succeeds or FAILED after max | 2026-08-10, real `createSaleWithOutbox()` + `processEbmOutboxBatch()` against a deliberately unreachable branch `vsdcUrl`. Attempt 1: connection failure → `status=FAILED`, `retryCount=1`, `lastError="fetch failed"`, backoff `nextAttemptAt` scheduled. Restored URL, forced retry: `status=SUCCEEDED`, fiscalized normally. | **PASS** |
| TC-05 | Invalid credentials | Wrong `EBM_API_KEY` | Complete sale | FAILED status, error stored, queue retry | 2026-08-10, local sandbox, TIN `000000000` → correctly rejected, resultCd 910 "tin: Must be a valid TIN" | **PASS** |
| TC-06 | Full refund after fiscal sale | Sale with SUCCESS EBM | Refund full amount | REFUND operation submitted, `EbmTransaction` for refund | 2026-08-10, local sandbox, orig invcNo 97595 → refund invcNo w/ `orgInvcNo=97595`, `rcptTyCd=R`, `salesSttsCd=05` → resultCd 000, rcptNo 48 | **PASS** |
| TC-07 | Cancel after fiscal sale | Sale with SUCCESS EBM | Cancel sale | VOID operation submitted | 2026-08-10, local sandbox. Bug found: void resubmitted the *same* `invcNo` → resultCd 924 "Invoice number already exists." Root cause: void must use a **fresh** `invcNo` (like refund gets from a separate Sale row) with `orgInvcNo` left at 0 (`orgInvcNo` is refund-only — sandbox rejects it on a void with resultCd 910). **Fixed** in `ebm-outbox.service.ts` and `rra-ebm.service.ts` (`buildRraSendReceiptPayload` now takes `invcNoOverride`, void paths allocate one via `generateInvoiceNumber()`). Re-ran end-to-end after the fix: sale rcptNo 52 → void rcptNo 53, resultCd 000. | **PASS (after fix)** |
| TC-08 | Refund without EBM success | Sale never fiscalized | Refund | No refund API call (no-op) | 2026-08-10, real `processEbmOutboxBatch()` against a REFUND row whose original sale had no `EbmTransaction` at all. No HTTP call made (confirmed: no `EbmTransaction` row created either — the defer check runs before that). Row is deferred with `lastError="Original invoice not yet fiscalized — deferring refund"` and retried on backoff, not silently dropped — different from VOID, which permanently marks itself `SUCCEEDED`/skipped in the same situation. Both correctly never call the gateway; only the terminal state differs. | **PASS** |
| TC-09 | Mock mode | `EBM_USE_MOCK=true` | Sale | SUCCESS with `MOCK-EBM-*`, no HTTP | 2026-08-10, `EBM_USE_MOCK=true` with `EBM_API_URL` pointed at an unreachable host. `saveInvc()` returned SUCCESS with `MOCK-INTERNAL-*`/`MOCK-SIG-*` refs in ~2ms — confirms no network attempt is made. | **PASS** |
| TC-10 | Per-org invoice sequence | Two orgs create sales | Create sales in parallel | Distinct `INV-*` sequences per org | 2026-08-10, 8 concurrent `generateInvoiceNumber()` calls each for two different orgs (org 2/branch 3, org 3/branch 4). No duplicate `vsdcInvcNo` within either org; sequences fully independent (org 2 continued from 5030s, org 3 started fresh at 1–8). | **PASS** |

## Notes

- Replace path and body field names with those from the **official RRA VSDC specification** before submission.
- For production certification, repeat TC-01–TC-08 in the **production** environment after RRA approval.
- **TC-03 follow-up:** confirm the checkout/customer flow always collects a valid 6-character `prcOrdCd` before a B2B (non-7-prefix TIN) sale reaches `submitSaleToEbm`, otherwise every B2B fiscal submission will be rejected in production the same way.
