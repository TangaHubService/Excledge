VITE_PUBLIC_API_URL=http://localhost:5000  # /api is appended automatically
VITE_WS_URL=ws://localhost:5000
VITE_APP_URL=http://localhost:5173
VITE_CONTACT_FORM_SCRIPT_ID=

# AI invoice scanning (OpenAI vision, called directly from the browser). This
# key ships inside the client bundle — scope it to a low-spend project.
VITE_OPENAI_API_KEY=
VITE_OPENAI_VISION_MODEL=gpt-4o
